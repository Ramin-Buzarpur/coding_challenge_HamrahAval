apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-api-client-config
data:
  # Comma separated list of node base URLs.
  NODE_HOSTS: "http://node1.example.com,http://node2.example.com,http://node3.example.com"
  REQUEST_TIMEOUT: "5"
  MAX_ATTEMPTS: "3"
  LOG_LEVEL: "INFO"
